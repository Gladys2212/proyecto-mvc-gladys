let resultado;

resultado = Math.PI;
resultado = Math.round(2.5); // redondea el numero
resultado = Math.ceil(25.3); // redondea hacia arriba
resultado = Math.floor(99.8); // redondea hacia abajo
resultado = Math.sqrt(400); // devuelve la raiz cuadrada
resultado = Math.abs(-45); // convierte el numero a positivo
resultado = Math.pow(2, 6); // saca la potencia al numero asignado
resultado = Math.min(100, 15, 4, 8); // devuelve el numero menor
resultado = Math.max(100, 15, 4, 8); // devuelve el numero mayor
resultado = Math.cbrt(27); // devuelve el numero elevado al cubo

console.log(resultado);
